# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循语义化版本。版本由 git tag（`v0.9.x`）承载，代码内不嵌版本字符串。

## [未发布]

### 新增

- **Cubase 路径自动探测**：不再钉死 Cubase 15 安装路径——启动时扫
  `Program Files\Steinberg` 各版本目录取版本号最高的 `Cubase<N>.exe`；
  `config.json` 钉的路径已不存在（升级 Cubase/换机）也自动回退探测值，
  不再静默失效（显式留空仍表示不自动拉起）。
- **开发工程化**：补 MIT LICENSE；新增 GitHub Actions CI（每次 push 跑全部
  源码语法检查 + `test_bridge.py` 离线自检）；README 增补「开发 / 构建」
  环境说明；e2e_test.py 改为动态扫描工程库取最短两首（不再写死本机歌名与
  路径，换机设环境变量 `CUBE_PROJECTS_ROOT` 覆盖库根）。

### 修复

- **启动时长全量重探**：启动时长探测此前只补「从未测过」的歌，在 Cubase 里
  改过定位条（时长）的歌重启后仍显示旧值或「未知」。改为启动全量重读自动
  来源的 .cpr（实测 65 首/66MB 约 0.15s），手动「写入时长」仍不被覆盖。
- **Cubase 退出尾巴误判「已在运行」**：退出本软件连带关闭 Cubase 后立刻重开
  本软件，Cubase 进程尚在退出收尾（界面已无）会被误报「已在运行」而不拉起。
  启动自检改为：进程在而界面全无 → 等其退净（至多 45 秒）再自动启动；
  等待期间界面出现（如正处冷启动）则仍按「已在运行」处理。
- **VJ 音符回调不再做网络请求**：OBS 动作曾直接跑在 winmm 回调线程，OBS 卡顿
  时拖住同端口时钟投递（>0.25s 即误判暂停、视频抖动），退出等回调也会「未响应」。
  改为回调只入队、串行工作线程执行，积压只留最新触发。
- **退出立即关窗**：熄屏/停 OBS 挪到退出后的后台收尾线程（非守护，进程等它
  做完），主线程不再等网络；先熄屏再按需关闭被控软件（反了熄屏会发不出去）。
- **稳定性杂项**：单实例互斥体（双开弹窗提示后退出）；日志上限 2000 行（长
  演出不减速）；playlist.json 并发写加锁；音色映射文件改原子写；打包关闭
  UPX（杀软扫描/冷启动更快，偶发误报）。
- **踩钉学习排除所有 loopMIDI 端口**：按 loopMIDI 注册表端口名单识别虚拟口
  （改名/新增自动覆盖），不再硬编码端口名；名字含 loopMIDI 作兜底；
  排除名单只剩硬件琴（JUNO/AX-09/Lucina）。

## [0.9.6] - 2026-09-24

### 新增

- **Windows 标准安装程序**：`build.bat` 末尾自动编译
  `dist\CubeSetlistManager-Setup-<版本>.exe`（Inno Setup 6，版本取最近 git tag）。
  按用户安装到 `%LOCALAPPDATA%\Programs\CubeSetlistManager`（免管理员），含
  开始菜单/桌面快捷方式与卸载器；升级重装保留 exe 目录已有配置。安装包只含
  程序本体 + `config.example.json` + README，不含 config.json/playlist.json/
  crash.log 等运行时真实数据。Release 资产改为安装程序，不再发 zip。

### 移除

- **VJ Automator 独立程序**：删除 `midi_bridge_gui.py`、`VJ Automator.spec`
  及 dist/build 产物，Cube Setlist Manager 成为唯一产品；`midi_bridge.py`
  （主程序内嵌 VJ 联动的核心）保留。夜测/渲染自检的相关用例同步清理
  （GUI 回归 56→55 项）。

## [0.9.5] - 2026-09-24

### 更改

- **键盘自动化窗口乐器分页**：内容一列过长，改为顶部「乐器」下拉切换
  （JUNO DS-88 / AX-09 Lucina），窗口只显示当前琴的音色槽、移调/延音键，
  高度随页内容自适应。
- **键盘自动化窗口信息简化**：标题固定为「键盘自动化」（不再拼接歌曲名），
  去掉「配置目标：XXX」状态行；底部端口状态行只显示当前乐器那行，并省略
  「JUNO：」「AX-09：」前缀。
- **退出被控软件并行关闭**：退出时 OBS/loopMIDI 不再排队等 Cubase 先退完
  （Cubase 退出本身要几十秒），三者同时发关闭请求、各自兜底；loopMIDI 经
  真机实测 WM_CLOSE 只缩托盘不退出，改为直接终止（虚拟 MIDI 口无用户数据）。

### 修复

- **键盘自动化分页往返切换报错**：从 AX-09 切回 JUNO（及后续往返）时
  TclError「window … isn't packed」——分页布局改为全部重排落尾，不再用
  before= 锚定可能正被隐藏的端口行。

## [0.9.4] - 2026-09-24

### 新增

- **延音踏板触发键**：键盘自动化新增 E2(40)→JUNO、A2(45)→AX-09 两个延音
  触发键（中间 F2–G#2 四键留给以后 AX-09 音高移动）：按住=向琴发延音 CC64
  踩下、松开=抬起，按住多久延音多久；重叠按住按计数处理，全部松开才抬起；
  「键盘自动化」窗口与全局移调同款固定行展示。延音消息与音色切换同端口
  同通道发送。注意 JUNO-DS88 的延音只作用于从 MIDI 进来的音符（官方实现
  如此），控制不了手弹延音。
- **AX-09 移调真机探针** `probe_ax09_shift.py` 入库：逐项发 RPN Coarse/Fine/
  弯音范围听音验证，用于实测 AX-09 的移调接收能力。

### 修复

- **自动化动作失败提示不带动作名**：监控栏原来只显示端口错误（如「切换失败：
  未找到含…」），看不出是哪个动作失败；现在带动作名（音色描述/延音踩下抬起）。

## [0.9.3] - 2026-09-24

### 新增

- **MIDI 端口联动可选停用**：设置页「VJ 端口名称」「键盘端口名称」下拉新增
  「无」（与「VJ显示位置」同款式），选中即停用该自动化——保存即热切换停听，
  改回端口即恢复；停用的口启动不再监听。config 以空串 `vjPortHint`/`kbPortHint`
  表示停用，旧配置缺键仍回默认端口。
- **失效设定幽灵项标注**：三个下拉对「配置里记得、当前环境已不在场」的设定
  （显示器被拔/设备未上电/端口改名）显式标注「名字（当前不可用）」，不再是
  真假难辨的普通选项；不动它保存=保留原设定（设备恢复自动接回），改选其它项
  即替换；VJ/键盘同名失效提示在下拉中去重。

### 修复

- **两个联动都停用时 loopMIDI 仍会自动拉起**：loopMIDI 承载 Cubase 工程的
  时钟端口，不在场会让工程每次载入弹「未找到端口」；停用联动只跳过等端口
  就绪，不再跳过拉起（loopMIDI 不在进程列表才补拉）。

## [0.9.2] - 2026-09-23

### 新增

- **OBS 节目投影**：设置页「VJ显示位置」选择显示器，把 OBS 节目画面全屏投影过去；
  换屏先关旧投影不留双份；屏名优先按名匹配、对不上 OBS 命名时按屏幕排列排名兜底；
  OBS 重连后自动恢复投影；obs-websocket 5.x 无关闭投影请求，走 WM_CLOSE 兜底。
- **VJ 静音播放**：设置页勾选=媒体源静音+关监听；不勾=开「监视器并输出」出声；
  随每次 OBS 连接同步。
- **退出时关闭被控软件**（设置页可选）：按 Cubase → OBS → loopMIDI 次序优雅关闭，
  全程 WM_CLOSE 不强杀（Cubase 未保存确认框回车=保存；loopMIDI 先关后终止）。
- **投影真机探针** `_probe_projector.py`：只读 dry-run 校验屏名能否对上 OBS 显示器，
  不弹投影窗。

### 修复

- **OBS 重启后自动续播上次的视频**：根治——熄屏时停媒体源并清空其文件
  （媒体源只要存着文件，随场景激活就会播，无「启动不自动播放」开关）；
  冷启动拉起的 OBS 连上后也清一次残留。
- **切工程弹窗吞键**：打开工程阶段出现确认类弹窗时，回车确认后补发被模态框
  吞掉的按键；弹窗处理抽为模块级 `cubase_ctrl._drain`，退出流程复用同一套。
- **高 DPI 下三个子窗（设置/键盘自动化/踩钉）边距顶满**：pack 边距一律过
  `dpi.scale` 换算；表头与数据列左缘对齐。
- **子窗面板色斑**：新增 `dpi.flatten`，表单页文字/容器底色统一为窗口底色。

### 变更

- **品牌更名**：「工程播放台」→ **Cube Setlist Manager**，「MIDI 视频桥」→
  **VJ Automator**（窗口标题、spec、build.bat、测试与 docs 全量同步）。
- 设置页联动端口从手填提示名改为**在线端口下拉**（缺端口保留占位项）。
- VJ Automator 走带跟随状态格播放/暂停时显示**当前视频名**。
- `_render_check.py` 新增回归：走带跟随四态、表头对齐、子窗四边留白随 DPI、
  子窗无面板色斑。

## [0.9.0] - 2026-09-23

首版入库：

- 歌单编排 + Cubase 工程「先关后开」切换 + 播完两段式自动推进；
- JUNO-DS / AX-09 键盘自动化音色映射录制与触发；CC 踩钉学习/热插拔；
- VJ 视频桥（走带跟随、熄屏）+ OBS 自动拉起/断线重连 + 缺源自动补建；
- 启动自检三大后端；深色大字演出界面；`build.bat` 数据安全打包流程。
